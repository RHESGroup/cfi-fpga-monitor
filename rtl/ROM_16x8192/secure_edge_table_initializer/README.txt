Usage of the tool:

The file: "unsecure_arch.txt" contains the list of all unsercure arch and is thereby composed as follows:
	each line contains an arch
	each arch is composed by the start label and the arrive label the two label are divided by a blank space

The python script read from the file "unsecure_arch.txt" and write the results in "secure_edge_table.mem" 